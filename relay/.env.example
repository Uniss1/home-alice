# API Key for Agent authentication
# SECURITY: NEVER use the placeholder value in production!
# Generate with: openssl rand -hex 32
API_KEY=your-secret-api-key-here

# Listen address inside container
LISTEN_ADDR=:8080
